================
enable_edit_form
================

.. js:function:: enable_edit_form()

**domain**: client 

**language**: javascript

**class** :doc:`Item class </refs/client/item_api>`

Description
===========

在调用 :doc:`disable_edit_form <m_disable_edit_form>` 方法之后，
调用 ``enable_edit_form`` 方法以重新启用编辑表单窗体，

Example
=======

.. code-block:: js

  function on_edit_form_created(item) {
      var save_btn = item.add_edit_button('Save and continue');
      save_btn.click(function() {
          if (item.is_changing()) {
              item.disable_edit_form();
              item.post();
              item.apply(function(error){
                  if (error) {
                      item.alert_error(error);
                  }
                  item.edit();
                  item.enable_edit_form();
              });
          }
      });
  }

See also
========

:doc:`disable_edit_form <m_disable_edit_form>` 
